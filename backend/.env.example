# Backend Environment Variables
# Copie este arquivo para .env e configure os valores apropriados

# ============================================
# SERVIDOR
# ============================================
PORT=3001
NODE_ENV=development

# ============================================
# BANCO DE DADOS
# ============================================
# URL de conexão com PostgreSQL (formato Prisma)
# Formato: postgresql://usuario:senha@host:porta/database?schema=public
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agroxp?schema=public

# Alternativa: variáveis individuais (se não usar DATABASE_URL)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=agroxp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ============================================
# AUTENTICAÇÃO E SEGURANÇA
# ============================================
# IMPORTANTE: Altere estes valores para produção!
# Use um gerador de secrets seguro (ex: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-here-change-in-production
JWT_EXPIRES_IN=24h

REFRESH_TOKEN_SECRET=your-super-secret-refresh-token-key-here-change-in-production
REFRESH_TOKEN_EXPIRES_IN=7d

# ============================================
# CORS E ORIGENS PERMITIDAS
# ============================================
# Em produção, configure as origens permitidas separadas por vírgula
# Exemplo: ALLOWED_ORIGINS=https://app.agroxp.com,https://www.agroxp.com
ALLOWED_ORIGINS=

# URL do frontend (usado como fallback se ALLOWED_ORIGINS não estiver configurado)
FRONTEND_URL=http://localhost:5173

# ============================================
# RATE LIMITING (Opcional)
# ============================================
# Configurações de rate limiting podem ser adicionadas aqui se necessário
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# LOGGING (Opcional)
# ============================================
# LOG_LEVEL=info
# LOG_FORMAT=json

# ============================================
# OUTRAS CONFIGURAÇÕES
# ============================================
# Timeout para requisições (em milissegundos)
# REQUEST_TIMEOUT=60000
